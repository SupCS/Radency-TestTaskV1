# Kanban board

## Start working

Start with docker:
docker-compose up --build

Start locally:

cd backend && npm install
npm run start:dev

cd ../frontend && npm install
npm start

### Need to have

Node.js
NPM
PostgreSQL
Docker - optional
